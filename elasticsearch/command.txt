#Da utilizzare solo la prima volta che viene avviato il docker compose

curl -X PUT "http://localhost:9200/yt-sentiment" -H 'Content-Type: application/json' -d '{
  "settings": {
    "number_of_shards": 1,
    "number_of_replicas": 0
  }
}'